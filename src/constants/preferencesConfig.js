// Static configuration options for workspace preferences and onboarding.
// These are app-level constants (not workspace-specific data) used in dropdowns and forms.
import { DEFAULT_MEMBER_TYPES } from "@/lib/memberTypeService";

export const businessTypes = [
  "Photography",
  "Videography",
  "Event Management",
  "Production House"
];

export const teamMemberTypes = DEFAULT_MEMBER_TYPES;

export const themes = ["Contact Sheet", "Night", "Pastel"];