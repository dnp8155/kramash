{
  "name": "TeamMember",
  "type": "object",
  "properties": {
    "workspace_id": {
      "type": "string",
      "description": "Workspace this team member belongs to"
    },
    "name": {
      "type": "string",
      "description": "Team member name"
    },
    "phone": {
      "type": "string",
      "description": "Primary contact number"
    },
    "email": {
      "type": "string",
      "description": "Contact email"
    },
    "profession": {
      "type": "string",
      "description": "Profession / role label (snapshot of role name, or free text)"
    },
    "role_id": {
      "type": "string",
      "description": "Reference to TeamRole"
    },
    "is_self": {
      "type": "boolean",
      "default": false,
      "description": "Whether this member represents the workspace owner / business operator. SELF members are excluded from external team wage obligations."
    },
    "member_type_id": {
      "type": "string",
      "description": "Optional reference to a workspace member-type configuration (e.g. Bride Side, Groom Side, Common). Free-text ID; not a separate entity."
    },
    "default_rate": {
      "type": "number",
      "default": 0,
      "description": "Default rate for this team member. Used as a suggestion when assigning to events; does NOT overwrite existing assignment agreed_rate."
    },
    "rate_type": {
      "type": "string",
      "enum": [
        "Per Event",
        "Per Day",
        "Fixed"
      ],
      "default": "Per Event"
    },
    "notes": {
      "type": "string",
      "description": "Free-form notes about this team member"
    },
    "status": {
      "type": "string",
      "enum": [
        "Active",
        "Inactive"
      ],
      "default": "Active"
    }
  },
  "required": [
    "workspace_id",
    "name"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.workspace_id": {
            "$in": "{{user.data.workspace_ids}}"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "data.workspace_id": {
        "$in": "{{user.data.workspace_ids}}"
      }
    },
    "update": {
      "$or": [
        {
          "data.workspace_id": {
            "$in": "{{user.data.workspace_ids}}"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.workspace_id": {
            "$in": "{{user.data.workspace_ids}}"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}