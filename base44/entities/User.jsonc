{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "phone": {
      "type": "string",
      "title": "Phone Number",
      "description": "Phone number for OTP login (E.164 format, e.g. +91...)"
    },
    "language": {
      "type": "string",
      "title": "App Language",
      "description": "Preferred UI language code (en, hi, gu)",
      "default": "en",
      "enum": [
        "en",
        "hi",
        "gu"
      ]
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}