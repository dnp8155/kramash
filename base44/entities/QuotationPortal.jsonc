{
  "name": "QuotationPortal",
  "type": "object",
  "properties": {
    "workspace_id": {
      "type": "string",
      "description": "Workspace this portal belongs to"
    },
    "quotation_id": {
      "type": "string",
      "description": "Quotation this portal exposes publicly"
    },
    "public_token": {
      "type": "string",
      "description": "Secure random token used in the public URL. Not enumerable."
    },
    "is_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Master on/off switch. If false, the public URL shows an unavailable message and exposes no quotation data."
    },
    "view_count": {
      "type": "number",
      "default": 0,
      "description": "Total number of valid portal openings"
    },
    "first_viewed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the first valid portal view"
    },
    "last_viewed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the most recent valid portal view"
    }
  },
  "required": [
    "workspace_id",
    "quotation_id",
    "public_token"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}