{
  "name": "enableClientPortalAccess",
  "entry": "entry.ts",
  "description": "Workspace admin enables (or disables) password-only portal access for a client. On enable, auto-generates a random access token + password and returns the plaintext password once for the admin to share."
}